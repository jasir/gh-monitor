#!/bin/bash

# GitHub Actions Monitor Script
# Sleduje stav GitHub Action run a notifikuje při dokončení

set -e

# Barvy pro output
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

# Funkce pro zobrazení nápovědy
show_help() {
    echo "GitHub Actions Monitor"
    echo ""
    echo "Usage: $0 <run_id> [options]"
    echo ""
    echo "Parametry:"
    echo "  run_id              ID GitHub Action run (povinné)"
    echo ""
    echo "Volby:"
    echo "  -r, --repo REPO     Repository ve formátu owner/repo (výchozí: aktuální repo)"
    echo "  -i, --interval SEC  Interval kontroly v sekundách (výchozí: 10)"
    echo "  -t, --timeout MIN   Timeout v minutách (výchozí: 60)"
    echo "  -q, --quiet         Tichý režim - pouze konečná notifikace"
    echo "  -h, --help          Zobrazí tuto nápovědu"
    echo ""
    echo "Příklady:"
    echo "  $0 16648023936"
    echo "  $0 16648023936 --repo KosikGroup/web-frontend"
    echo "  $0 16648023936 --interval 10 --timeout 120"
}

# Funkce pro notifikaci
notify() {
    local title="$1"
    local message="$2"
    local urgency="${3:-normal}"
    local url="${4:-}"
    local notification_type="${5:-normal}"  # low nebo normal
    
    echo ""
    echo "🔔 ========================================"
    echo "   $title"
    echo "   $message"
    if [[ -n "$url" ]]; then
        echo "   🔗 $url"
    fi
    echo "========================================"
    echo ""
    
    # Desktop notifikace - vždy se pokusí odeslat
    if command -v notify-send >/dev/null 2>&1 && [[ -n "$DISPLAY" ]]; then
        # Odebrání escape sekvencí pro čistý text v notifikaci
        local clean_message=$(echo -e "$message" | sed 's/\\033\[[0-9;]*m//g')
        
        # Přidání URL do notifikace pokud existuje
        if [[ -n "$url" ]]; then
            clean_message="$clean_message\n\n🔗 Zobrazit: $url"
        fi
        
        # Rozhodování o typu notifikace
        if [[ "$notification_type" == "low" ]]; then
            # LOW notifikace - zmizí po 5 sekundách
            if notify-send -t 5000 -u "normal" "$title" "$clean_message" 2>/dev/null; then
                echo "📱 Low notifikace odeslána (zmizí za 5s)"
            else
                echo "⚠️  Desktop notifikace nedostupná (není GUI prostředí)"
            fi
        elif [[ "$urgency" == "critical" ]] || [[ "$urgency" == "completion" ]]; then
            # NORMAL/CRITICAL notifikace - persistent, vyžadují ruční zavření
            if notify-send -t 0 -u "critical" "$title" "$clean_message" 2>/dev/null; then
                echo "📱 Persistent notifikace odeslána (vyžaduje ruční zavření)"
            else
                echo "⚠️  Desktop notifikace nedostupná (není GUI prostředí)"
            fi
        else
            # NORMAL notifikace - persistent
            if notify-send -t 0 -u "normal" "$title" "$clean_message" 2>/dev/null; then
                echo "📱 Normal notifikace odeslána (vyžaduje ruční zavření)"
            else
                echo "⚠️  Desktop notifikace nedostupná (není GUI prostředí)"
            fi
        fi
    else
        echo "⚠️  Desktop notifikace nedostupná (chybí notify-send nebo DISPLAY)"
    fi
    
    # Audio signál pro dokončení
    if [[ "$urgency" == "critical" ]] && command -v paplay >/dev/null 2>&1; then
        paplay /usr/share/sounds/alsa/Front_Left.wav 2>/dev/null || true
    elif [[ "$urgency" == "critical" ]] && command -v beep >/dev/null 2>&1; then
        beep -f 800 -l 300 2>/dev/null || true
    fi
}

# Funkce pro určení typu notifikace podle workflow a stavu
get_notification_type() {
    local workflow_name="$1"
    local status="$2"
    local conclusion="$3"
    
    # NORMAL (persistent) - vždy vyžadují pozornost
    if [[ "$status" == "waiting" ]]; then
        echo "normal"  # čeká na approval
    elif [[ "$conclusion" == "failure" ]] || [[ "$conclusion" == "cancelled" ]] || [[ "$conclusion" == "timed_out" ]]; then
        echo "normal"  # jakákoliv chyba
    elif [[ "$workflow_name" =~ [Dd]eploy.*[Pp]rod|[Pp]roduction|[Pp]rod ]]; then
        echo "normal"  # produkční deploy
    elif [[ "$workflow_name" =~ [Ii]ntegr|[Ee]2[Ee]|[Aa]cceptance ]]; then
        echo "normal"  # integrační/E2E testy
    else
        # LOW (auto-hide) - úspěšné běžné operace
        if [[ "$conclusion" == "success" ]]; then
            echo "low"  # úspěšné běžné testy/buildy
        else
            echo "normal"  # ostatní případy raději persistent
        fi
    fi
}

# Funkce pro získání stavu run
get_run_status() {
    local run_id="$1"
    local repo="$2"
    
    if [[ -n "$repo" ]]; then
        gh run view "$run_id" --repo "$repo" --json status,conclusion,name,createdAt,url,jobs
    else
        gh run view "$run_id" --json status,conclusion,name,createdAt,url,jobs
    fi
}

# Funkce pro zobrazení aktuálních jobů
show_current_jobs() {
    local jobs_json="$1"
    local max_jobs=3
    
    if [[ "$jobs_json" == "null" ]] || [[ -z "$jobs_json" ]]; then
        return
    fi
    
    # Najdi běžící joby
    local running_jobs=$(echo "$jobs_json" | jq -r '.[] | select(.status == "in_progress") | .name')
    # Najdi joby ve frontě
    local queued_jobs=$(echo "$jobs_json" | jq -r '.[] | select(.status == "queued") | .name')
    
    if [[ -n "$running_jobs" ]]; then
        echo -e "   ${BLUE}🔄 Běžící kroky:${NC}"
        echo "$running_jobs" | head -$max_jobs | while read -r job; do
            [[ -n "$job" ]] && echo "     • $job"
        done
        
        local running_count=$(echo "$running_jobs" | wc -l)
        if [[ $running_count -gt $max_jobs ]]; then
            echo "     ... a dalších $((running_count - max_jobs)) kroků"
        fi
    fi
    
    if [[ -n "$queued_jobs" ]]; then
        echo -e "   ${YELLOW}⏳ Ve frontě:${NC}"
        echo "$queued_jobs" | head -2 | while read -r job; do
            [[ -n "$job" ]] && echo "     • $job"
        done
        
        local queued_count=$(echo "$queued_jobs" | wc -l)
        if [[ $queued_count -gt 2 ]]; then
            echo "     ... a dalších $((queued_count - 2)) kroků"
        fi
    fi
}

# Funkce pro zobrazení stavu
show_status() {
    local status="$1"
    local conclusion="$2"
    local name="$3"
    local run_id="$4"
    local elapsed="$5"
    local jobs_json="$6"
    
    case "$status" in
        "completed")
            case "$conclusion" in
                "success")
                    echo -e "${GREEN}✅ DOKONČENO - ÚSPĚCH${NC}"
                    ;;
                "failure")
                    echo -e "${RED}❌ DOKONČENO - CHYBA${NC}"
                    ;;
                "cancelled")
                    echo -e "${YELLOW}🚫 DOKONČENO - ZRUŠENO${NC}"
                    ;;
                *)
                    echo -e "${YELLOW}⚠️  DOKONČENO - $conclusion${NC}"
                    ;;
            esac
            ;;
        "in_progress")
            echo -e "${BLUE}🔄 BĚŽÍ${NC}"
            ;;
        "queued")
            echo -e "${YELLOW}⏳ VE FRONTĚ${NC}"
            ;;
        "waiting")
            echo -e "${RED}⏸️  ČEKÁ NA AKCI UŽIVATELE${NC}"
            ;;
        *)
            echo -e "${YELLOW}❓ NEZNÁMÝ STAV: $status${NC}"
            ;;
    esac
    
    echo "   Workflow: $name"
    echo "   Run ID: $run_id"
    echo "   Uplynulý čas: $(format_time $elapsed)"
    
    # Zobraz aktuální kroky pokud workflow běží nebo čeká
    if [[ "$status" == "in_progress" ]] || [[ "$status" == "queued" ]] || [[ "$status" == "waiting" ]]; then
        show_current_jobs "$jobs_json"
    fi
}

# Funkce pro formátování času
format_time() {
    local seconds=$1
    local hours=$((seconds / 3600))
    local minutes=$(((seconds % 3600) / 60))
    local secs=$((seconds % 60))
    
    if [[ $hours -gt 0 ]]; then
        printf "%dh %dm %ds" $hours $minutes $secs
    elif [[ $minutes -gt 0 ]]; then
        printf "%dm %ds" $minutes $secs
    else
        printf "%ds" $secs
    fi
}

# Hlavní funkce
main() {
    local run_id=""
    local repo=""
    local interval=10
    local timeout=3600  # 60 minut v sekundách
    local quiet=false
    
    # Parsování argumentů
    while [[ $# -gt 0 ]]; do
        case $1 in
            -h|--help)
                show_help
                exit 0
                ;;
            -r|--repo)
                repo="$2"
                shift 2
                ;;
            -i|--interval)
                interval="$2"
                shift 2
                ;;
            -t|--timeout)
                timeout=$((${2} * 60))  # převod na sekundy
                shift 2
                ;;
            -q|--quiet)
                quiet=true
                shift
                ;;
            *)
                if [[ -z "$run_id" ]]; then
                    run_id="$1"
                else
                    echo "Neznámý parametr: $1"
                    exit 1
                fi
                shift
                ;;
        esac
    done
    
    # Kontrola povinných parametrů
    if [[ -z "$run_id" ]]; then
        echo "Chyba: Musíte zadat run_id"
        echo ""
        show_help
        exit 1
    fi
    
    # Ověření, že gh je dostupné
    if ! command -v gh >/dev/null 2>&1; then
        echo "Chyba: GitHub CLI (gh) není nainstalované"
        exit 1
    fi
    
    # Ověření, že jq je dostupné
    if ! command -v jq >/dev/null 2>&1; then
        echo "Chyba: jq není nainstalované (potřebné pro parsování JSON)"
        echo "Nainstalujte: sudo apt install jq"
        exit 1
    fi
    
    # Ověření autentifikace
    if ! gh auth status >/dev/null 2>&1; then
        echo "Chyba: Nejste přihlášeni do GitHub CLI"
        echo "Spusťte: gh auth login"
        exit 1
    fi
    
    echo "🚀 Spouštím sledování GitHub Action run..."
    echo "   Run ID: $run_id"
    [[ -n "$repo" ]] && echo "   Repository: $repo"
    echo "   Interval: ${interval}s"
    echo "   Timeout: $(format_time $timeout)"
    echo ""
    
    local start_time=$(date +%s)
    local check_count=0
    
    while true; do
        local current_time=$(date +%s)
        local elapsed=$((current_time - start_time))
        
        # Kontrola timeout
        if [[ $elapsed -gt $timeout ]]; then
            notify "GitHub Action Timeout" "Sledování run $run_id bylo ukončeno kvůli timeout" "critical" "" "normal"
            exit 1
        fi
        
        check_count=$((check_count + 1))
        
        # Získání stavu
        local status_json
        if ! status_json=$(get_run_status "$run_id" "$repo" 2>/dev/null); then
            echo "❌ Chyba při získávání stavu run $run_id"
            sleep $interval
            continue
        fi
        
        local status=$(echo "$status_json" | jq -r '.status')
        local conclusion=$(echo "$status_json" | jq -r '.conclusion // "null"')
        local name=$(echo "$status_json" | jq -r '.name')
        local url=$(echo "$status_json" | jq -r '.url')
        local jobs_json=$(echo "$status_json" | jq -r '.jobs')
        
        # Zobrazení stavu (pokud není tichý režim)
        if [[ "$quiet" == false ]]; then
            echo "📊 Kontrola #$check_count ($(date '+%H:%M:%S'))"
            show_status "$status" "$conclusion" "$name" "$run_id" "$elapsed" "$jobs_json"
            echo ""
        fi
        
        # Kontrola změny stavu z waiting
        if [[ "$status" != "waiting" ]] && [[ -f "/tmp/gh-monitor-${run_id}-waiting" ]]; then
            rm -f "/tmp/gh-monitor-${run_id}-waiting"
            local notif_type=$(get_notification_type "$name" "$status" "$conclusion")
            notify "🔔 GitHub Action pokračuje" "Workflow: <b>$name</b>\nZměna ze stavu čekání\nRun ID: $run_id" "normal" "$url" "$notif_type"
        elif [[ "$status" == "waiting" ]] && [[ ! -f "/tmp/gh-monitor-${run_id}-waiting" ]]; then
            touch "/tmp/gh-monitor-${run_id}-waiting"
            notify "⏸️ GitHub Action čeká" "Workflow: <b>$name</b>\nPotřebuje akci uživatele\nRun ID: $run_id" "critical" "$url" "normal"
        fi
        
        # Kontrola, zda je run dokončen
        if [[ "$status" == "completed" ]]; then
            local duration=$(format_time $elapsed)
            
            case "$conclusion" in
                "success")
                    local title="✅ GitHub Action ÚSPĚŠNÁ"
                    local message="Workflow: <b>$name</b>\nDoba běhu: <i>$duration</i>\nRun ID: $run_id"
                    local notif_type=$(get_notification_type "$name" "$status" "$conclusion")
                    notify "$title" "$message" "completion" "$url" "$notif_type"
                    echo "🎉 Workflow dokončena úspěšně!"
                    exit 0
                    ;;
                "failure")
                    local title="❌ GitHub Action SELHALA"
                    local message="Workflow: <b>$name</b>\nDoba běhu: <i>$duration</i>\nRun ID: $run_id"
                    local notif_type=$(get_notification_type "$name" "$status" "$conclusion")
                    notify "$title" "$message" "completion" "$url" "$notif_type"
                    echo "💥 Workflow selhala!"
                    echo "🔗 Zobrazit na GitHubu: $url"
                    exit 1
                    ;;
                "cancelled")
                    local title="🚫 GitHub Action ZRUŠENA"
                    local message="Workflow: <b>$name</b>\nDoba běhu: <i>$duration</i>\nRun ID: $run_id"
                    local notif_type=$(get_notification_type "$name" "$status" "$conclusion")
                    notify "$title" "$message" "completion" "$url" "$notif_type"
                    echo "⏹️  Workflow byla zrušena"
                    exit 2
                    ;;
                *)
                    local title="⚠️ GitHub Action dokončena"
                    local message="Workflow: <b>$name</b>\nStav: <u>$conclusion</u>\nDoba běhu: <i>$duration</i>\nRun ID: $run_id"
                    local notif_type=$(get_notification_type "$name" "$status" "$conclusion")
                    notify "$title" "$message" "completion" "$url" "$notif_type"
                    echo "❓ Workflow dokončena s neočekávaným stavem: $conclusion"
                    exit 3
                    ;;
            esac
        fi
        
        # Čekání před další kontrolou
        sleep $interval
    done
}

# Spuštění hlavní funkce
main "$@"